spring:
  mvc.view:
    prefix: /WEB-INF/
    suffix: .jsp

moviefun:
  datasource:
    movies:
      url: jdbc:mysql://localhost:3306/movies?useSSL=false
      username: root
      password:
    albums:
      url: jdbc:mysql://localhost:3306/albums?useSSL=false
      username: root
      password:
